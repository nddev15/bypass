{
  "name": "bypass-api-vercel",
  "version": "1.0.0",
  "description": "Link Bypass API - Vercel Serverless",
  "main": "api/index.js",
  "scripts": {
    "start": "vercel dev",
    "dev": "vercel dev",
    "build": "npm install",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": "18.x"
  }
}
